import 'package:flutter/material.dart';

class Konnecting extends StatelessWidget {
  const Konnecting({super.key});
  final String serverUrl = 'https://localhost:3000';
  final String apiRoute = 'auth/v1/items';

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
